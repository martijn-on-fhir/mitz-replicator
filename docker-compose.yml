services:
  mitz-replicator:
    build: .
    ports:
      - "8443:8443"
    environment:
      - PORT=8443
      - MTLS_ENABLED=false
      - SAML_VALIDATION_ENABLED=false
      # - SAML_SIGNING_CERT=certs/client.crt
      # - SAML_EXPECTED_ISSUER=
      # - SAML_CLOCK_SKEW_SECONDS=5
    # Uncomment to mount your own certificates instead of the generated ones:
    # volumes:
    #   - ./certs:/app/certs:ro